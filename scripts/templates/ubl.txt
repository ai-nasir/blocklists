---
name: ${ title }
description: ${ description }
homepage: ${ project_url }
version: ${ version }
---
% for entry in entries:
  % if entry.allow:
@*://*.${ entry.hostname }/*
  % else:
*://*.${ entry.hostname }/*
  %endif
% endfor