---
name: ${ title }
description: ${ description }
homepage: ${ project_url }
version: ${ version }
---
% for entry in entries:
    % if entry.allow:
@\
    % endif
    % if entry.exact:
*://\
    % else:
*://*.\
    % endif
${ entry.hostname }/*
% endfor